from django.urls import include, re_path
from django.views import generic

from . import views


urlpatterns = [
    # re_path('^$', generic.RedirectView.as_view(url='./mymodel/'), name="index"),
    # re_path('^mymodel/', include(views.MyModelViewSet().urls)),
]
